diff --git a/node_modules/@mux/mux-react-native-player/ios/MuxPreloadManager.swift b/node_modules/@mux/mux-react-native-player/ios/MuxPreloadManager.swift
index 4ac2bac..710ea7c 100644
--- a/node_modules/@mux/mux-react-native-player/ios/MuxPreloadManager.swift
+++ b/node_modules/@mux/mux-react-native-player/ios/MuxPreloadManager.swift
@@ -7,9 +7,12 @@ import MuxPlayerSwift
 ///
 /// A plain `AVPlayer` has no cross-item media cache, so unlike Android the
 /// warmed bytes live inside the buffered `AVPlayerItem` itself. The pool
-/// therefore hands the item across: when a `MuxVideoView` receives a source
-/// whose playback-affecting fields match a pooled entry, it adopts the warmed
-/// item — and the buffer it accumulated — instead of building a cold one.
+/// therefore hands the entire player across: when a `MuxVideoView` receives a
+/// source whose playback-affecting fields match a pooled entry, it adopts the
+/// warmed player and its current item instead of attempting to attach that item
+/// to a second player. AVFoundation forbids one item from belonging to two
+/// players, and detaching then immediately reattaching can still trip that
+/// invariant because the ownership transition is not synchronous.
 ///
 /// DRM sources are rejected before an item is created: a FairPlay license
 /// exchange outside an active playback session could consume single-use
@@ -23,7 +26,6 @@ final class MuxPreloadManager {
     let playbackId: String
     let playbackFingerprint: String
     let player: AVPlayer
-    let item: AVPlayerItem
   }
 
   /// Preloads beyond this are evicted oldest-first; keeps memory bounded.
@@ -64,23 +66,21 @@ final class MuxPreloadManager {
       Entry(
         playbackId: playbackId,
         playbackFingerprint: source.playbackFingerprint,
-        player: player,
-        item: item
+        player: player
       )
     )
   }
 
-  /// Pops the warmed item for a source whose playback-affecting fields match.
+  /// Pops the warmed player for a source whose playback-affecting fields match.
   /// Metadata-only differences (Mux Data fields) do not prevent adoption, which
   /// is why this matches on ``MuxVideoSourceRecord/playbackFingerprint`` rather
   /// than the full source fingerprint.
-  func takeItem(matchingPlaybackFingerprint fingerprint: String) -> AVPlayerItem? {
+  func takePlayer(matchingPlaybackFingerprint fingerprint: String) -> AVPlayer? {
     guard let index = entries.firstIndex(where: { $0.playbackFingerprint == fingerprint }) else {
       return nil
     }
     let entry = entries.remove(at: index)
-    entry.player.replaceCurrentItem(with: nil)
-    return entry.item
+    return entry.player
   }
 
   func cancel(playbackId: String) {
diff --git a/node_modules/@mux/mux-react-native-player/ios/MuxVideoView.swift b/node_modules/@mux/mux-react-native-player/ios/MuxVideoView.swift
index 0300e13..b06814c 100644
--- a/node_modules/@mux/mux-react-native-player/ios/MuxVideoView.swift
+++ b/node_modules/@mux/mux-react-native-player/ios/MuxVideoView.swift
@@ -105,15 +105,17 @@ final class MuxVideoView: ExpoView {
 
     configurePlaybackAudioSession()
 
-    if let warmedItem = MuxPreloadManager.shared.takeItem(
+    if let warmedPlayer = MuxPreloadManager.shared.takePlayer(
       matchingPlaybackFingerprint: source.playbackFingerprint
     ) {
-      // Adopt the preloaded item and the buffer it already accumulated.
+      // Adopt the preloaded player and the buffer its item already accumulated.
       // `prepare(...)` always constructs a fresh AVPlayerItem, which would
-      // throw that buffer away, so the player is assembled here and Mux Data
-      // monitoring is registered directly. The preload path never accepts DRM
-      // sources, so no FairPlay session registration is needed on this branch.
-      playerViewController.player = AVPlayer(playerItem: warmedItem)
+      // throw that buffer away. Transferring the whole AVPlayer also avoids
+      // associating its AVPlayerItem with two players, which AVFoundation treats
+      // as a fatal programmer error. Mux Data monitoring is registered directly;
+      // the preload path never accepts DRM sources, so no FairPlay registration
+      // is needed on this branch.
+      playerViewController.player = warmedPlayer
       let monitoringOptions = source.toMonitoringOptions()
       MUXSDKStats.monitorAVPlayerViewController(
         playerViewController,
